<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xxk.management.stock.dao.StockDao">
    <sql id="columns">id,stock_ident,entity_id,stock_type,stock_office_id,stock_no,stock_unit,stock_proportion,stock_total,
        stock_flag,remark,keyWord,stock_version,createUserId,createDate,updateUserId,updateDate,deleteFlag</sql>

    <!--获取分页 -->
    <select id="listStock" parameterType="string" resultType="com.xxk.management.stock.entity.Stock">
        select
        <include refid="columns"></include>
        from xxk_stock where deleteFlag='0'and stock_type=#{2} ORDER BY createDate desc limit #{0},#{1};
    </select>

    <!--根据id获取信息 -->
    <select id="listStockByEntityId" parameterType="string" resultType="com.xxk.management.stock.entity.Stock">
        select
        <include refid="columns"></include>
        from xxk_stock
        where entity_id=#{0}
        and stock_office_id=#{1}
        and deleteFlag='0';
    </select>

    <!--添加信息 --><!--设置版本号为1-->
    <insert id="addStock" parameterType="com.xxk.management.stock.entity.Stock">
        insert into xxk_stock(<include refid="columns"></include>) values(
        #{id},#{stock_ident},#{entity_id},#{stock_type},#{stock_office_id},#{stock_no},#{stock_unit},#{stock_proportion},#{stock_total},
        #{stock_flag},#{remark},#{keyWord},1,#{createUserId},#{createDate},#{updateUserId},#{updateDate},#{deleteFlag});
    </insert>

    <!--根据id获取信息 -->
    <select id="getStockIdByIdent" parameterType="string" resultType="string">
        select id
        from xxk_stock
        where in_confirmed_ident=#{0}
        and deleteFlag='0';
    </select>

    <!--根据id获取信息 -->
    <select id="getStockById" parameterType="string" resultType="com.xxk.management.stock.entity.Stock">
        select
        <include refid="columns"></include>
        from xxk_stock
        where id=#{0}
        and deleteFlag='0';
    </select>

    <!--更新记录 -->
    <update id="updateStock" parameterType="com.xxk.management.stock.entity.Stock">
        update xxk_stock set account=#{account},birth=#{birth},sex=#{sex},phone=#{phone},createUnit=#{createUnit},
        QCellCore=#{QCellCore},updateUserId=#{updateUserId},updateDate=#{updateDate}
        where entity_id=#{entity_id} and deleteFlag='0';
    </update>

    <!--入库更新 -->
    <update id="plusStockNo" parameterType="com.xxk.management.stock.entity.Stock">
        update xxk_stock set stock_no=#{stock_no},stock_total=stock_total+#{stock_total},stock_version=stock_version+1,
        updateUserId=#{updateUserId},updateDate=#{updateDate} where id=#{id} and stock_version=#{stock_version} and deleteFlag='0';
    </update>

    <!--批量修改（批量逻辑删除） -->
    <!--以下的批量方法只能删除数据表的一样数据-->
    <update id="deleteListRegUser" parameterType="java.util.List">
        UPDATE xxk_reg_user SET deleteFlag ='1' WHERE id IN
        <foreach collection="list" index="index" item="item" open="(" separator="," close=")">
            #{item}
        </foreach>
    </update>

</mapper>